/// Modelo de sala (room).
///
/// Stub de la Fase 1. Se completa en la Fase 3 con `Room.fromJson`
/// para parsear la respuesta de `GET /rooms/list` y `GET /rooms/<id>`.
class Room {
  const Room();
}
