import 'package:flutter/material.dart';

/// Detalle de una sala.
///
/// Stub de la Fase 1. Se completa en la Fase 3 (detalle/eliminar) y
/// en la Fase 4 (grabación y envío de audio).
class RoomDetailScreen extends StatelessWidget {
  const RoomDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Room detail screen - pendiente Fase 3/4')),
    );
  }
}
