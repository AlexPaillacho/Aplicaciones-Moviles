import 'package:flutter/material.dart';

/// Lista de salas.
///
/// Stub de la Fase 1. Se completa en la Fase 3 con el `ListView`,
/// pull-to-refresh y creación de salas.
class RoomsListScreen extends StatelessWidget {
  const RoomsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Rooms list screen - pendiente Fase 3')),
    );
  }
}
