import 'api_service.dart';

/// Servicio de salas (rooms) contra el backend Flask.
///
/// Stub de la Fase 1. Se completa en la Fase 3 (listar/crear/editar/
/// eliminar salas) y en la Fase 4 (envío de audio y consulta de estado
/// de tarea).
class RoomsService {
  RoomsService(this._apiService);

  final ApiService _apiService;
}
