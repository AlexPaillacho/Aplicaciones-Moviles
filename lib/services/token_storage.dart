/// Wrapper sobre almacenamiento seguro del token JWT.
///
/// Stub de la Fase 1. Se completa en la Fase 2 con
/// `FlutterSecureStorage` (`saveToken`, `readToken`, `deleteToken`).
class TokenStorage {
  const TokenStorage();
}
